<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Reviews - TaskNet</title>
    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,%3Csvg viewBox='0 0 24 24' xmlns='http://www.w3.org/2000/svg'%3E%3Crect width='24' height='24' fill='%23667eea' rx='4'/%3E%3Cpath d='M9 11H7v2h2v-2zm4 0h-2v2h2v-2zm4 0h-2v2h2v-2zm2-7h-1V2h-2v2H8V2H6v2H5c-1.11 0-1.99.9-1.99 2L3 20c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 16H5V9h14v11z' fill='white'/%3E%3C/svg%3E" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #f4f7f9; margin: 0; padding: 0; min-height: 100vh; display: flex; flex-direction: column; }
        .top-header {
            background-color: #ffffff;
            padding: 1rem 2rem;
            border-bottom: 1px solid #e5e7eb;
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-shrink: 0;
        }
        .footer {
            border-top: 1px solid #e5e7eb;
            background-color: white;
            padding: 0.75rem 0;
            text-align: center;
            font-size: 0.875rem;
            color: #6b7280;
            flex-shrink: 0;
        }

        .header-profile {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid #e5e7eb;
            box-shadow: 0 2px 6px rgba(0,0,0,0.1);
            cursor: pointer;
        }

        .top-title {
            font-size: 1.875rem;
            font-weight: 700;
            color: #1f2937;
        }
        .page-container { display: flex; flex: 1; overflow: hidden; }
        .sidebar {
            width: 256px;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            padding: 1.5rem;
            background-color: #ffffff;
            border-right: 1px solid #e5e7eb;
            box-sizing: border-box;
        }
        .main-content { flex: 1; padding: 1.5rem 2rem; background-color: #ffffff; overflow-y: auto; display: flex; flex-direction: column; }
        .nav-title { font-size: 1.125rem; font-weight: 600; color: #374151; margin-bottom: 15px; }
        .nav-link {
            display: block;
            padding: 1rem;
            margin-bottom: 0.5rem;
            border-radius: 0.5rem;
            color: #4b5563;
            font-weight: 600;
            transition: all 0.15s ease-in-out;
            text-decoration: none;
        }
        .nav-link:hover {
            background-color: #f3f4f6;
            color: #1f2937;
        }
        .nav-link.active {
            background-color: #e5e7eb;
            color: #1f2937;
        }
        .logout-button {
            display: flex;
            align-items: center;
            justify-content: flex-start;
            width: 45px;
            height: 45px;
            border: none;
            border-radius: 50%;
            cursor: pointer;
            position: relative;
            overflow: hidden;
            transition-duration: 0.3s;
            box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.199);
            background-color: white;
            margin-top: 2rem;
        }

        .logout-button .sign {
            width: 100%;
            transition-duration: 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .logout-button .sign svg {
            width: 17px;
        }

        .logout-button .sign svg path {
            fill: black;
        }

        .logout-button .text {
            position: absolute;
            right: 0%;
            width: 0%;
            opacity: 0;
            color: white;
            font-size: 1.2em;
            font-weight: 600;
            transition-duration: 0.3s;
            letter-spacing: 0.05em;
        }

        .logout-button:hover {
            background-color: black;
            width: 155px;
            border-radius: 40px;
            transition-duration: 0.3s;
        }

        .logout-button:hover .sign {
            width: 25%;
            transition-duration: 0.3s;
            padding-left: 15px;
        }

        .logout-button:hover .sign svg path {
            fill: white;
        }

        .logout-button:hover .text {
            opacity: 1;
            width: 75%;
            transition-duration: 0.3s;
            padding-right: 15px;
            text-align: center;
        }

        .logout-button:active {
            transform: translate(2px, 2px);
        }

        .header-title { font-size: 1.5rem; font-weight: 700; color: #0f172a; margin-bottom: 1.5rem; }
        
        .filter-section { 
            background-color: #f8fafc; 
            padding: 1.5rem; 
            border-radius: 0.75rem; 
            margin-bottom: 1.5rem; 
            border: 1px solid #e2e8f0;
        }
        
        .filter-row {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 1rem;
            margin-bottom: 1rem;
        }
        
        .filter-group {
            display: flex;
            flex-direction: column;
        }
        
        .filter-label { 
            font-weight: 600; 
            color: #374151; 
            font-size: 0.9rem; 
            margin-bottom: 0.5rem;
        }
        
        input[type="text"], select {
            padding: 0.6rem;
            border: 1px solid #d1d5db;
            border-radius: 0.375rem;
            font-size: 0.9rem;
            font-family: inherit;
        }
        
        input[type="text"]:focus, select:focus {
            outline: none;
            border-color: #064e3b;
            box-shadow: 0 0 0 3px rgba(6, 78, 59, 0.1);
        }
        
        .filter-actions {
            display: flex;
            gap: 0.75rem;
            align-items: flex-end;
        }
        
        .btn-search {
            background-color: #064e3b;
            color: white;
            padding: 0.6rem 1.5rem;
            border-radius: 0.375rem;
            font-weight: 600;
            border: none;
            cursor: pointer;
            transition: background-color 0.2s;
            font-size: 0.9rem;
        }
        .btn-search:hover { background-color: #065f46; }
        
        .btn-clear {
            background-color: #f8fafc;
            color: #334155;
            padding: 0.6rem 1.5rem;
            border-radius: 0.375rem;
            font-weight: 600;
            border: 1px solid #e2e8f0;
            cursor: pointer;
            transition: background-color 0.2s;
            font-size: 0.9rem;
        }
        .btn-clear:hover { background-color: #f1f5f9; }
        
        .table-wrapper {
            border: 1px solid #e2e8f0;
            border-radius: 0.75rem;
            overflow-x: auto;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            background-color: white;
        }
        
        thead {
            background-color: #f8fafc;
            border-bottom: 2px solid #e2e8f0;
        }
        
        th {
            padding: 1rem;
            text-align: left;
            font-weight: 600;
            color: #374151;
            font-size: 0.9rem;
        }
        
        td {
            padding: 1rem;
            border-bottom: 1px solid #e2e8f0;
            color: #475569;
            font-size: 0.9rem;
        }
        
        tbody tr:hover {
            background-color: #f8fafc;
        }
        
        .student-name {
            font-weight: 600;
            color: #1e293b;
        }
        
        .status-badge {
            display: inline-block;
            padding: 0.375rem 0.75rem;
            border-radius: 9999px;
            font-size: 0.8rem;
            font-weight: 600;
        }
        
        .badge-submitted { background-color: #fef3c7; color: #92400e; }
        .badge-approved { background-color: #dcfce7; color: #166534; }
        .badge-rejected { background-color: #fee2e2; color: #991b1b; }
        
        .date-cell { color: #64748b; font-size: 0.85rem; }
        
        .no-data {
            text-align: center;
            padding: 3rem;
            color: #64748b;
            font-style: italic;
        }
        
        .pagination {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 0.5rem;
            margin-top: 1.5rem;
            padding: 1rem;
        }
        
        .pagination a, .pagination span {
            padding: 0.5rem 0.75rem;
            border: 1px solid #e2e8f0;
            border-radius: 0.375rem;
            text-decoration: none;
            color: #334155;
            font-weight: 600;
            transition: all 0.2s;
            font-size: 0.9rem;
        }
        
        .pagination a:hover {
            background-color: #f8fafc;
            border-color: #cbd5e1;
        }
        
        .pagination .active {
            background-color: #064e3b;
            color: white;
            border-color: #064e3b;
        }
        
        .pagination .disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }

        @media (max-width: 1024px) {
            .main-content { padding: 1.25rem 1.5rem; }
            .header-title { font-size: 1.25rem; }
            .filter-row { grid-template-columns: repeat(3, 1fr); gap: 0.75rem; }
            .filter-actions { gap: 0.5rem; }
            .btn-search, .btn-clear { padding: 0.6rem 1rem; font-size: 0.85rem; }
            th, td { padding: 0.75rem; font-size: 0.85rem; }
        }

        @media (max-width: 768px) {
            .page-container {
                flex-direction: column;
                min-height: auto;
                width: 100%;
                box-sizing: border-box;
            }
            .top-header {
                padding: 0.75rem 1rem;
                gap: 0.5rem;
                align-items: center;
                position: sticky;
                top: 0;
                z-index: 100;
            }

            .top-title {
                font-size: 1.1rem;
                margin: 0;
                flex: 1;
                white-space: nowrap;
                overflow: hidden;
                text-overflow: ellipsis;
            }

            .header-profile {
                width: 36px;
                height: 36px;
                flex-shrink: 0;
            }

            .sidebar {
                width: 100% !important;
                border-right: none !important;
                border-bottom: none !important;
                padding: 0 !important;
                margin: 0 !important;
                position: fixed !important;
                top: 70px !important;
                left: 0 !important;
                right: 0 !important;
                z-index: 90 !important;
                max-height: 0 !important;
                overflow: hidden !important;
                flex-direction: column !important;
                transition: max-height 0.25s ease-out !important;
                transition:  box-shadow 0.25s ease-out !important;
                background-color: #ffffff !important;
                justify-content: flex-start !important;
                box-sizing: border-box !important;
                display: flex !important;
            }

            .sidebar.open {
                max-height: 400px !important;
                padding: 1rem !important;
                border-bottom: 1px solid #e5e7eb !important;
                box-shadow: 0 4px 6px rgba(0,0,0,0.1) !important;
                overflow: auto !important;
            }

            .sidebar > div:first-child {
                display: flex;
                flex-direction: column;
                gap: 1rem;
            }

            .nav-title {
                margin-bottom: 0.5rem;
                font-size: 0.9rem;
            }

            nav {
                display: flex;
                flex-direction: column;
                gap: 0.5rem;
            }

            .nav-link {
                padding: 0.6rem 1rem;
                margin-bottom: 0 !important;
                font-size: 0.85rem;
            }

            .logout-button {
                margin-top: 1rem;
                width: 40px;
                height: 40px;
            }

            .main-content { 
                padding: 1rem;
                margin-top: 0;
                width: 100%;
                box-sizing: border-box;
                flex: 1;
            }
            .header-title { font-size: 1.25rem; }
            .filter-section { padding: 1rem; margin-bottom: 1rem; }
            .filter-row { grid-template-columns: 1fr; gap: 0.5rem; margin-bottom: 0.75rem; }
            .filter-actions { flex-direction: column; }
            .btn-search, .btn-clear { width: 100%; padding: 0.6rem; font-size: 0.85rem; }
            .filter-label { font-size: 0.8rem; }
            input[type="text"], select { font-size: 0.85rem; padding: 0.5rem; }
            .table-wrapper { overflow-x: auto; }
            th, td { padding: 0.75rem; font-size: 0.8rem; }
            .status-badge { padding: 0.3rem 0.6rem; font-size: 0.75rem; }
            .pagination { gap: 0.3rem; padding: 0.75rem; }
            .pagination a, .pagination span { padding: 0.4rem 0.6rem; font-size: 0.8rem; }
        }

        @media (max-width: 480px) {
            body { 
                min-height: 100vh; 
            }
            .top-header { 
                padding: 0.5rem 0.75rem;
                height: 50px;
            }
            .top-title { 
                font-size: 1rem; 
            }
            .sidebar {
                top: 60px !important;
            }

            .sidebar.open {
                max-height: 500px !important;
                padding: 0.75rem !important;
                overflow: auto !important;
            }

            .sidebar > div:first-child {
                gap: 0.75rem;
            }

            nav {
                gap: 0.25rem;
            }

            .nav-title { 
                font-size: 0.85rem; 
                margin-bottom: 0.5rem; 
                font-weight: 600; 
            }
            .nav-link {
                padding: 0.5rem 0.75rem;
                margin-bottom: 0;
                font-size: 0.75rem;
                font-weight: 600;
            }
            .logout-button { 
                width: 40px; 
                height: 40px; 
                margin-top: 0.5rem; 
            }
            .main-content { 
                padding: 0.75rem; 
            }
            .header-title { 
                font-size: 1rem; 
                margin-bottom: 0.75rem; 
            }
            .filter-section { 
                padding: 0.75rem; 
                margin-bottom: 1.5rem;
                box-sizing: border-box;
            }
            .filter-row { 
                grid-template-columns: 1fr; 
                gap: 0.4rem; 
                margin-bottom: 0.5rem; 
            }
            .filter-label { 
                font-size: 0.75rem; 
                margin-bottom: 0.3rem; 
            }
            input[type="text"], select { 
                font-size: 0.8rem; 
                padding: 0.5rem; 
                border-radius: 0.3rem;
                box-sizing: border-box;
            }
            .filter-actions { 
                flex-direction: column; 
                width: 100%;
                gap: 0.4rem;
                align-items: stretch;
            }
            .btn-search, .btn-clear { 
                width: 100%; 
                padding: 0.5rem; 
                font-size: 0.75rem;
                box-sizing: border-box;
            }
            .table-wrapper { 
                border-radius: 0.5rem; 
            }
            table { 
                font-size: 0.8rem; 
            }
            th { 
                padding: 0.6rem; 
                font-size: 0.75rem; 
            }
            td { 
                padding: 0.6rem; 
                font-size: 0.75rem; 
            }
            .student-name { 
                font-size: 0.8rem; 
            }
            .date-cell { 
                font-size: 0.7rem; 
            }
            .status-badge { 
                padding: 0.25rem 0.5rem; 
                font-size: 0.7rem; 
            }
            .no-data { 
                padding: 1.5rem 0.5rem; 
                font-size: 0.8rem; 
            }
            .pagination { 
                gap: 0.2rem; 
                padding: 0.5rem; 
                flex-wrap: wrap;
            }
            .pagination a, .pagination span { 
                padding: 0.3rem 0.5rem; 
                font-size: 0.7rem; 
            }
        }

        .menu-toggle {
            display: none;
            background: none;
            border: none;
            cursor: pointer;
            font-size: 1.5rem;
            color: #1f2937;
            padding: 0;
            width: 40px;
            height: 40px;
            align-items: center;
            justify-content: center;
        }

        @media (max-width: 768px) {
            .menu-toggle {
                display: flex;
            }
        }
    </style>
</head>
<body>
    <header class="top-header">
        <button class="menu-toggle" id="menuToggle">☰</button>
        <h1 class="top-title">Professor Portal</h1>
        <img src="/professor.png" alt="Profile" class="header-profile">
    </header>

    <div class="page-container">
        <div class="sidebar">
            <div>
                <div class="nav-title">Navigation</div>
                <nav>
                    <a href="/professor/dashboard" class="nav-link <%= activePage === 'dashboard' ? 'active' : '' %>">
                        Dashboard
                    </a>
                    <a href="/professor/reviews" class="nav-link <%= activePage === 'reviews' ? 'active' : '' %>">
                        My Reviews
                    </a>
                    <a href="/professor/profile" class="nav-link <%= activePage === 'profile' ? 'active' : '' %>">
                        Profile
                    </a>
                </nav>
            </div>
            <form action="/logout" method="POST" style="margin-top: 2rem;">
                <button type="submit" class="logout-button">
                    <div class="sign">
                        <svg viewBox="0 0 512 512">
                            <path d="M377.9 105.9L500.7 228.7c7.2 7.2 11.3 17.1 11.3 27.3s-4.1 20.1-11.3 27.3L377.9 406.1c-6.4 6.4-15 9.9-24 9.9c-18.7 0-33.9-15.2-33.9-33.9l0-62.1-128 0c-17.7 0-32-14.3-32-32l0-64c0-17.7 14.3-32 32-32l128 0 0-62.1c0-18.7 15.2-33.9 33.9-33.9c9 0 17.6 3.6 24 9.9zM160 96L96 96c-17.7 0-32 14.3-32 32l0 256c0 17.7 14.3 32 32 32l64 0c17.7 0 32 14.3 32 32s-14.3 32-32 32l-64 0c-53 0-96-43-96-96L0 128C0 75 43 32 96 32l64 0c17.7 0 32 14.3 32 32s-14.3 32-32 32z"></path>
                        </svg>
                    </div>
                    <div class="text">Logout</div>
                </button>
            </form>
        </div>

        <div class="main-content">
            <h1 class="header-title">My Reviews</h1>

            <div class="filter-section">
                <form method="GET" action="/professor/reviews">
                    <div class="filter-row">
                        <div class="filter-group">
                            <label class="filter-label">Search</label>
                            <input type="text" name="search" placeholder="Student name or assignment..." value="<%= searchQuery %>">
                        </div>
                        <div class="filter-group">
                            <label class="filter-label">Status</label>
                            <select name="status">
                                <option value="All" <%= statusFilter === 'All' ? 'selected' : '' %>>All</option>
                                <option value="Submitted" <%= statusFilter === 'Submitted' ? 'selected' : '' %>>Submitted</option>
                                <option value="Approved" <%= statusFilter === 'Approved' ? 'selected' : '' %>>Approved</option>
                                <option value="Rejected" <%= statusFilter === 'Rejected' ? 'selected' : '' %>>Rejected</option>
                            </select>
                        </div>
                        <div class="filter-group">
                            <label class="filter-label">Date Range</label>
                            <select name="dateRange">
                                <option value="all" <%= dateRange === 'all' ? 'selected' : '' %>>All Time</option>
                                <option value="week" <%= dateRange === 'week' ? 'selected' : '' %>>Last Week</option>
                                <option value="month" <%= dateRange === 'month' ? 'selected' : '' %>>Last Month</option>
                                <option value="quarter" <%= dateRange === 'quarter' ? 'selected' : '' %>>Last 3 Months</option>
                            </select>
                        </div>
                        <div class="filter-actions">
                            <button type="submit" class="btn-search">Search</button>
                            <a href="/professor/reviews" class="btn-clear">Clear</a>
                        </div>
                    </div>
                </form>
            </div>

            <div class="table-wrapper">
                <% if (assignments && assignments.length > 0) { %>
                    <table>
                        <thead>
                            <tr>
                                <th>Assignment Title</th>
                                <th>Course Code</th>
                                <th>Student Name</th>
                                <th>Status</th>
                                <th>Submitted Date</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% assignments.forEach(assignment => { %>
                                <tr>
                                    <td><strong><%= assignment.title %></strong></td>
                                    <td><%= assignment.courseCode || 'N/A' %></td>
                                    <td class="student-name"><%= assignment.studentId.name %></td>
                                    <td>
                                        <span class="status-badge badge-<%= assignment.status.toLowerCase() %>">
                                            <%= assignment.status %>
                                        </span>
                                    </td>
                                    <td class="date-cell"><%= new Date(assignment.submissionDate).toLocaleDateString() %></td>
                                    <td>
                                        <% if (assignment.status === 'Submitted') { %>
                                            <a href="/professor/assignments/<%= assignment._id %>/review" style="color: #064e3b; font-weight: 600; text-decoration: none;">Review</a>
                                        <% } else { %>
                                            <span style="color: #94a3b8;">-</span>
                                        <% } %>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                <% } else { %>
                    <div class="no-data">
                        No reviews found matching your filters.
                    </div>
                <% } %>
            </div>

            <% if (totalPages > 1) { %>
                <div class="pagination">
                    <% if (currentPage > 1) { %>
                        <a href="/professor/reviews?page=1&search=<%= searchQuery %>&status=<%= statusFilter %>&dateRange=<%= dateRange %>">First</a>
                        <a href="/professor/reviews?page=<%= currentPage - 1 %>&search=<%= searchQuery %>&status=<%= statusFilter %>&dateRange=<%= dateRange %>">← Previous</a>
                    <% } else { %>
                        <span class="disabled">First</span>
                        <span class="disabled">← Previous</span>
                    <% } %>

                    <% for (let i = Math.max(1, currentPage - 2); i <= Math.min(totalPages, currentPage + 2); i++) { %>
                        <% if (i === currentPage) { %>
                            <span class="active"><%= i %></span>
                        <% } else { %>
                            <a href="/professor/reviews?page=<%= i %>&search=<%= searchQuery %>&status=<%= statusFilter %>&dateRange=<%= dateRange %>"><%= i %></a>
                        <% } %>
                    <% } %>

                    <% if (currentPage < totalPages) { %>
                        <a href="/professor/reviews?page=<%= currentPage + 1 %>&search=<%= searchQuery %>&status=<%= statusFilter %>&dateRange=<%= dateRange %>">Next →</a>
                        <a href="/professor/reviews?page=<%= totalPages %>&search=<%= searchQuery %>&status=<%= statusFilter %>&dateRange=<%= dateRange %>">Last</a>
                    <% } else { %>
                        <span class="disabled">Next →</span>
                        <span class="disabled">Last</span>
                    <% } %>
                </div>
            <% } %>
        </div>
    </div>

    <footer class="footer">
        &copy; 2025 TaskNet - Professor Portal. All rights reserved.
    </footer>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            // Mobile sidebar toggle
            const menuToggle = document.getElementById('menuToggle');
            const sidebar = document.querySelector('.sidebar');

            menuToggle.addEventListener('click', () => {
                sidebar.classList.toggle('open');
            });

            document.querySelectorAll('.nav-link').forEach(link => {
                link.addEventListener('click', () => {
                    sidebar.classList.remove('open');
                });
            });

            document.querySelector('.main-content').addEventListener('click', () => {
                sidebar.classList.remove('open');
            });
        });
    </script>
</body>
</html>
