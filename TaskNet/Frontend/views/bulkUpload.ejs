<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bulk Upload Assignments</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #f4f7f9; margin: 0; padding: 0; height: 100vh; display: flex; flex-direction: column; }
        
        .top-header {
            background-color: #ffffff;
            padding: 1rem 2rem;
            border-bottom: 1px solid #e5e7eb;
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-shrink: 0;
        }

        .header-profile {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid #e5e7eb;
            box-shadow: 0 2px 6px rgba(0,0,0,0.1);
            cursor: pointer;
        }

        .top-title {
            font-size: 1.875rem;
            font-weight: 700;
            color: #1f2937;
        }
        
        .page-container { display: flex; flex: 1; overflow: hidden; }
        .sidebar { width: 256px; display: flex; flex-direction: column; justify-content: space-between; padding: 1.5rem; background-color: #ffffff; border-right: 1px solid #e5e7eb; box-sizing: border-box; }
        .main-content { flex-grow: 1; padding: 2rem 3rem; background-color: #ffffff; overflow-y: auto; }

        .nav-title { font-size: 1.125rem; font-weight: 600; color: #374151; margin-bottom: 15px; }
        .nav-link {
            display: block;
            padding: 1rem;
            margin-bottom: 0.5rem;
            border-radius: 0.5rem;
            color: #4b5563;
            font-weight: 600;
            transition: all 0.15s ease-in-out;
            text-decoration: none;
        }
        .nav-link:hover {
            background-color: #f3f4f6;
            color: #1f2937;
        }
        .nav-link.active {
            background-color: #e5e7eb;
            color: #1f2937;
        }
        .logout-button {
            width: 100%;
            text-align: center;
            padding: 0.75rem 1rem;
            border-radius: 0.5rem;
            font-weight: 600;
            background-color: #ef4444;
            color: white;
            border: none;
            cursor: pointer;
            transition: background-color 0.2s;
            margin-top: 2rem;
        }
        .logout-button:hover {
            background-color: #dc2626;
        }

        .header-title { font-size: 2rem; font-weight: 700; color: #0f172a; margin-bottom: 0.5rem; }
        .header-sub { color: #64748b; margin-bottom: 2rem; }
        
        .form-card { max-width: 600px; }
        .form-group { margin-bottom: 1.5rem; }
        label { display: block; font-weight: 600; color: #374151; margin-bottom: 0.5rem; }
        
        input[type="text"], textarea, select, input[type="file"] {
            width: 100%; padding: 0.75rem; border: 1px solid #d1d5db; border-radius: 0.5rem;
            box-sizing: border-box; font-family: inherit;
        }
        
        .file-hint { font-size: 0.875rem; color: #6b7280; margin-top: 0.25rem; }
        
        .submit-btn {
            background-color: #4338ca; 
            color: white; padding: 0.75rem 2rem; border-radius: 0.5rem;
            font-weight: 600; border: none; cursor: pointer; font-size: 1rem; transition: background-color 0.2s;
        }
        .submit-btn:hover { background-color: #3730a3; }

        .error-msg { color: #dc2626; font-size: 0.875rem; margin-top: 0.25rem; }
        .success-alert { background-color: #dcfce7; color: #14532d; padding: 1rem; border-radius: 0.5rem; margin-bottom: 1.5rem; border: 1px solid #86efac; }
        .error-alert { background-color: #fee2e2; color: #991b1b; padding: 1rem; border-radius: 0.5rem; margin-bottom: 1.5rem; border: 1px solid #fca5a5; }
        
        .toggle-link { margin-top: 1rem; display: block; color: #4b5563; text-decoration: underline; font-size: 0.9rem; }

        .footer {
            border-top: 1px solid #e5e7eb;
            background-color: white;
            padding: 0.75rem 0;
            text-align: center;
            font-size: 0.875rem;
            color: #6b7280;
            flex-shrink: 0;
        }

        /* Summary Table Styles */
        .summary-table { width: 100%; border-collapse: collapse; margin-bottom: 2rem; border: 1px solid #e5e7eb; border-radius: 0.5rem; overflow: hidden; }
        .summary-table th, .summary-table td { padding: 1rem; text-align: left; border-bottom: 1px solid #e5e7eb; }
        .summary-table th { background-color: #f9fafb; font-weight: 600; color: #374151; }
        .status-badge { display: inline-block; padding: 0.25rem 0.75rem; border-radius: 1rem; font-size: 0.875rem; font-weight: 600; background-color: #bfdbfe; color: #1e3a8a; }
        .btn-secondary { background-color: #e5e7eb; color: #374151; padding: 0.75rem 1.5rem; border-radius: 0.5rem; font-weight: 600; text-decoration: none; margin-right: 1rem; }
    </style>
</head>
<body>

    <header class="top-header">
        <h1 class="top-title">Student Portal</h1>
        <img src="/student.png" alt="Profile" class="header-profile">
    </header>

    <div class="page-container">
        <div class="sidebar">
            <div>
                <div class="nav-title">Navigation</div>
                <nav>
                    <a href="/student/dashboard" class="nav-link <%= activePage === 'dashboard' ? 'active' : '' %>">Dashboard</a>
                    <a href="/student/assignments" class="nav-link <%= activePage === 'assignments' ? 'active' : '' %>">My Assignments</a>
                    <a href="/student/assignments/upload" class="nav-link <%= activePage === 'submit' ? 'active' : '' %>">Submit New</a>
                    <a href="#" class="nav-link">Profile</a>
                </nav>
            </div>
            <form action="/logout" method="POST">
                <button type="submit" class="logout-button">Logout</button>
            </form>
        </div>

        <div class="main-content">
            
            <% if (uploadedAssignments && uploadedAssignments.length > 0) { %>
                
                <h1 class="header-title">Upload Summary</h1>
                <div class="success-alert"><%= successMessage %></div>
                
                <p style="margin-bottom: 1.5rem; color: #4b5563;">The following assignments have been created with status "Draft":</p>

                <table class="summary-table">
                    <thead>
                        <tr>
                            <th>Title</th>
                            <th>Original File</th>
                            <th>Category</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% uploadedAssignments.forEach(assignment => { %>
                            <tr>
                                <td><%= assignment.title %></td>
                                <td><%= assignment.originalFileName %></td>
                                <td><%= assignment.category %></td>
                                <td><span class="status-badge"><%= assignment.status %></span></td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>

                <div style="display: flex;">
                    <a href="/student/dashboard" class="btn-secondary">Go to Dashboard</a>
                    <a href="/student/assignments/bulk-upload" class="submit-btn" style="text-decoration: none;">Upload More</a>
                </div>

            <% } else { %>
                <h1 class="header-title">Bulk Upload</h1>
                <p class="header-sub">Upload up to 5 files at once. They will share the same category and description.</p>

                <% if (successMessage) { %>
                    <div class="success-alert"><%= successMessage %></div>
                <% } %>
                
                <% if (errors.general) { %>
                    <div class="error-alert"><%= errors.general %></div>
                <% } %>

                <form action="/student/assignments/bulk-upload" method="POST" enctype="multipart/form-data" class="form-card">
                    
                    <div class="form-group">
                        <label for="title">Common Base Title*</label>
                        <input type="text" id="title" name="title" value="<%= formData.title || '' %>" placeholder="e.g., Final Project Files" required>
                        <div class="file-hint">Files will be saved as "Title (Part 1)", "Title (Part 2)", etc.</div>
                        <% if (errors.title) { %><div class="error-msg"><%= errors.title %></div><% } %>
                    </div>

                    <div class="form-group">
                        <label for="category">Category*</label>
                        <select id="category" name="category" required>
                            <option value="" disabled <%= !formData.category ? 'selected' : '' %>>Select Category</option>
                            <option value="Assignment" <%= formData.category === 'Assignment' ? 'selected' : '' %>>Assignment</option>
                            <option value="Thesis" <%= formData.category === 'Thesis' ? 'selected' : '' %>>Thesis</option>
                            <option value="Report" <%= formData.category === 'Report' ? 'selected' : '' %>>Report</option>
                        </select>
                        <% if (errors.category) { %><div class="error-msg"><%= errors.category %></div><% } %>
                    </div>

                    <div class="form-group">
                        <label for="description">Common Description (Optional)</label>
                        <textarea id="description" name="description" rows="3" placeholder="Description applied to all files..."><%= formData.description || '' %></textarea>
                    </div>

                    <div class="form-group">
                        <label for="assignmentFiles">Select Files (Max 5)*</label>
                        <input type="file" id="assignmentFiles" name="assignmentFiles" accept="application/pdf" multiple required>
                        <div class="file-hint">Accepted format: PDF only. Max 5 files.</div>
                        <% if (errors.file) { %><div class="error-msg"><%= errors.file %></div><% } %>
                    </div>

                    <button type="submit" class="submit-btn">Upload All Files</button>
                    
                    <a href="/student/assignments/upload" class="toggle-link">Switch to Single File Upload</a>
                </form>

            <% } %>
        </div>
    </div>

    <footer class="footer">
        &copy; 2025 TaskNet - Student Portal. All rights reserved.
    </footer>

</body>
</html>