<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Dashboard</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
        <style>
        body { font-family: 'Inter', sans-serif; background-color: #f4f7f9; margin: 0; padding: 0; height: 100vh; display: flex; flex-direction: column; }
        .top-header {
            background-color: #ffffff;
            padding: 1rem 2rem;
            border-bottom: 1px solid #e5e7eb;
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-shrink: 0;
        }
        .footer {
            border-top: 1px solid #e5e7eb;
            background-color: white;
            padding: 0.75rem 0;
            text-align: center;
            font-size: 0.875rem;
            color: #6b7280;
            flex-shrink: 0;
        }

        .header-profile {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid #e5e7eb;
            box-shadow: 0 2px 6px rgba(0,0,0,0.1);
            cursor: pointer;
        }

        .top-title {
            font-size: 1.875rem;
            font-weight: 700;
            color: #1f2937;
        }
        .page-container { display: flex; flex: 1; overflow: hidden; }
        .sidebar {
            width: 256px;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            padding: 1.5rem;
            background-color: #ffffff;
            border-right: 1px solid #e5e7eb;
            box-sizing: border-box;
        }
        .main-content { flex-grow: 1; padding: 1.5rem 2rem; background-color: #ffffff; overflow-y: auto; }
        .nav-title { font-size: 1.125rem; font-weight: 600; color: #374151; margin-bottom: 15px; }
        .nav-link {
            display: block;
            padding: 1rem;
            margin-bottom: 0.5rem;
            border-radius: 0.5rem;
            color: #4b5563;
            font-weight: 600;
            transition: all 0.15s ease-in-out;
            text-decoration: none;
        }
        .nav-link:hover {
            background-color: #f3f4f6;
            color: #1f2937;
        }
        .nav-link.active {
            background-color: #e5e7eb;
            color: #1f2937;
        }
        .logout-button {
            width: 100%;
            text-align: center;
            padding: 0.75rem 1rem;
            border-radius: 0.5rem;
            font-weight: 600;
            background-color: #ef4444;
            color: white;
            border: none;
            cursor: pointer;
            transition: background-color 0.2s;
            margin-top: 2rem;
        }
        .logout-button:hover {
            background-color: #dc2626;
        }
        .header-title { font-size: 1.5rem; font-weight: 700; color: #0f172a; margin-bottom: 0.75rem; }
        .stats-row { display: grid; grid-template-columns: repeat(4, 1fr); gap: 0.75rem; margin-bottom: 1.25rem }
        .stat-card { padding: 1rem; border-radius: 0.75rem; display: flex; flex-direction: column; justify-content: space-between; height: 120px; }
        .stat-title { font-size: 0.75rem; color: #64748b; font-weight: 500; margin-bottom: 0.4rem; }
        .stat-value { font-size: 2.25rem; font-weight: 600; color: #0f172a; }
        .bg-draft { background-color: #f0f4ff; }
        .bg-submitted { background-color: #f0fdf4; }
        .bg-approved { background-color: #fefce8; }
        .bg-rejected { background-color: #fef2f2; }
        .section-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 1rem; }
        .section-title { font-size: 1.1rem; font-weight: 600; color: #1e293b; }
        .upload-btn { background-color: #0f393b; color: white; padding: 0.5rem 1rem; border-radius: 0.5rem; font-weight: 600; text-decoration: none; display: inline-block; transition: background-color 0.2s; font-size: 0.9rem; }
        .upload-btn:hover { background-color: #0a2829; }
        .submission-list { display: flex; flex-direction: column; gap: 0.75rem; }
        .submission-item { display: flex; justify-content: space-between; align-items: center; padding: 1rem 0.9rem; border: 1px solid #e2e8f0; border-radius: 0.5rem; background-color: white; }
        .assignment-name { font-weight: 500; color: #334155; font-size: 0.9rem; }
        .status-badge { padding: 0.3rem 0.8rem; border-radius: 2rem; font-size: 0.75rem; font-weight: 600; min-width: 80px; text-align: center; }
        .badge-submitted { background-color: #6ee7b7; color: #064e3b; }
        .badge-approved { background-color: #dcfce7; color: #14532d; border: 1px solid #86efac; }
        .badge-rejected { background-color: #fca5a5; color: #7f1d1d; }
        .badge-draft { background-color: #bfdbfe; color: #1e3a8a; }
        .view-all-link { display: block; margin-top: 0.75rem; color: #0f393b; font-weight: 600; text-decoration: none; font-size: 0.85rem; }
        .view-all-link:hover { text-decoration: underline; }
    </style>
</head>
<body>
    <header class="top-header">
        <h1 class="top-title">Student Portal</h1>
        <img src="/student.png" alt="Profile" class="header-profile">
    </header>

    <div class="page-container">
        <div class="sidebar">
            <div>
                <div class="nav-title">Navigation</div>
                <nav>
                    <a href="/student/dashboard" class="nav-link <%= activePage === 'dashboard' ? 'active' : '' %>">
                        Dashboard
                    </a>
                    <a href="/student/assignments" class="nav-link <%= activePage === 'assignments' ? 'active' : '' %>">
                        All Assignments
                    </a>
                    <a href="#" class="nav-link">
                        Profile
                    </a>
                </nav>
            </div>
            <form action="/logout" method="POST" style="margin-top: 2rem;">
                <button type="submit" class="logout-button">Logout</button>
            </form>
        </div>
        <div class="main-content">
            <h1 class="header-title">Assignment Dashboard</h1>
            <div class="stats-row">
                <div class="stat-card bg-draft">
                    <div class="stat-title">Draft</div>
                    <div class="stat-value"><%= stats.Draft || 0 %></div>
                </div>
                <div class="stat-card bg-submitted">
                    <div class="stat-title">Submitted</div>
                    <div class="stat-value"><%= stats.Submitted || 0 %></div>
                </div>
                <div class="stat-card bg-approved">
                    <div class="stat-title">Approved</div>
                    <div class="stat-value"><%= stats.Approved || 0 %></div>
                </div>
                <div class="stat-card bg-rejected">
                    <div class="stat-title">Rejected</div>
                    <div class="stat-value"><%= stats.Rejected || 0 %></div>
                </div>
            </div>
            <div class="section-header">
                <div class="section-title">Recent Submissions</div>
                <a href="/student/assignments/upload" class="upload-btn">Upload New Assignment</a>
            </div>
            <div class="submission-list">
                <% if (recentAssignments && recentAssignments.length > 0) { %>
                    <% recentAssignments.forEach(assignment => { %>
                        <div class="submission-item">
                            <div class="assignment-name">
                                <%= assignment.title %>
                            </div>
                            <% 
                                let badgeClass = '';
                                switch(assignment.status) {
                                    case 'Submitted': badgeClass = 'badge-submitted'; break;
                                    case 'Approved': badgeClass = 'badge-approved'; break;
                                    case 'Rejected': badgeClass = 'badge-rejected'; break;
                                    default: badgeClass = 'badge-draft'; 
                                }
                            %>
                            <div class="status-badge <%= badgeClass %>">
                                <%= assignment.status %>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div class="submission-item" style="justify-content: center; color: #64748b;">
                        No assignments found. Start by uploading one!
                    </div>
                <% } %>
            </div>
            <a href="/student/assignments" class="view-all-link">View All Assignments →</a>
        </div>
    </div>
    <footer class="footer">
        &copy; 2025 Admin Dashboard. All rights reserved.
    </footer>
</body>
</html>
