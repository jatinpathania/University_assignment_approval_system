<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload - TaskNet</title>
    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,%3Csvg viewBox='0 0 24 24' xmlns='http://www.w3.org/2000/svg'%3E%3Crect width='24' height='24' fill='%23667eea' rx='4'/%3E%3Cpath d='M9 11H7v2h2v-2zm4 0h-2v2h2v-2zm4 0h-2v2h2v-2zm2-7h-1V2h-2v2H8V2H6v2H5c-1.11 0-1.99.9-1.99 2L3 20c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 16H5V9h14v11z' fill='white'/%3E%3C/svg%3E" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #f4f7f9; margin: 0; padding: 0; height: 100vh; display: flex; flex-direction: column; }
        
        .top-header {
            background-color: #ffffff;
            padding: 1rem 2rem;
            border-bottom: 1px solid #e5e7eb;
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-shrink: 0;
        }

        .header-profile {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid #e5e7eb;
            box-shadow: 0 2px 6px rgba(0,0,0,0.1);
            cursor: pointer;
        }

        .top-title {
            font-size: 1.875rem;
            font-weight: 700;
            color: #1f2937;
        }
        
        .page-container { display: flex; flex: 1; overflow: hidden; }
        .sidebar { width: 256px; display: flex; flex-direction: column; justify-content: space-between; padding: 1.5rem; background-color: #ffffff; border-right: 1px solid #e5e7eb; box-sizing: border-box; }
        .main-content { flex-grow: 1; padding: 2rem 3rem; background-color: #ffffff; overflow-y: auto; }

        .nav-title { font-size: 1.125rem; font-weight: 600; color: #374151; margin-bottom: 15px; }
        .nav-link {
            display: block;
            padding: 1rem;
            margin-bottom: 0.5rem;
            border-radius: 0.5rem;
            color: #4b5563;
            font-weight: 600;
            transition: all 0.15s ease-in-out;
            text-decoration: none;
        }
        .nav-link:hover {
            background-color: #f3f4f6;
            color: #1f2937;
        }
        .nav-link.active {
            background-color: #e5e7eb;
            color: #1f2937;
        }
        .logout-button {
            display: flex;
            align-items: center;
            justify-content: flex-start;
            width: 45px;
            height: 45px;
            border: none;
            border-radius: 50%;
            cursor: pointer;
            position: relative;
            overflow: hidden;
            transition-duration: 0.3s;
            box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.199);
            background-color: white;
            margin-top: 2rem;
        }

        .logout-button .sign {
            width: 100%;
            transition-duration: 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .logout-button .sign svg {
            width: 17px;
        }

        .logout-button .sign svg path {
            fill: black;
        }

        .logout-button .text {
            position: absolute;
            right: 0%;
            width: 0%;
            opacity: 0;
            color: white;
            font-size: 1.2em;
            font-weight: 600;
            transition-duration: 0.3s;
            letter-spacing: 0.05em;
        }

        .logout-button:hover {
            background-color: black;
            width: 155px;
            border-radius: 40px;
            transition-duration: 0.3s;
        }

        .logout-button:hover .sign {
            width: 25%;
            transition-duration: 0.3s;
            padding-left: 15px;
        }

        .logout-button:hover .sign svg path {
            fill: white;
        }

        .logout-button:hover .text {
            opacity: 1;
            width: 75%;
            transition-duration: 0.3s;
            padding-right: 15px;
            text-align: center;
        }

        .logout-button:active {
            transform: translate(2px, 2px);
        }

        .header-title { font-size: 2rem; font-weight: 700; color: #0f172a; margin-bottom: 2rem; }
        
        .form-card { max-width: 600px; background: white; padding: 0; }
        
        .form-group { margin-bottom: 1.5rem; }
        label { display: block; font-weight: 600; color: #374151; margin-bottom: 0.5rem; }
        
        input[type="text"], textarea, select, input[type="file"] {
            width: 100%; padding: 0.75rem; border: 1px solid #d1d5db; border-radius: 0.5rem;
            box-sizing: border-box; font-family: inherit;
        }
        
        .file-hint { font-size: 0.875rem; color: #6b7280; margin-top: 0.25rem; }
        
        .submit-btn {
            background-color: #0f393b; color: white; padding: 0.75rem 2rem; border-radius: 0.5rem;
            font-weight: 600; border: none; cursor: pointer; font-size: 1rem; transition: background-color 0.2s;
        }
        .submit-btn:hover { background-color: #0a2829; }

        .error-msg { color: #dc2626; font-size: 0.875rem; margin-top: 0.25rem; }
        .success-alert { background-color: #dcfce7; color: #14532d; padding: 1rem; border-radius: 0.5rem; margin-bottom: 1.5rem; border: 1px solid #86efac; }
        .error-alert { background-color: #fee2e2; color: #991b1b; padding: 1rem; border-radius: 0.5rem; margin-bottom: 1.5rem; border: 1px solid #fca5a5; }

        .footer {
            border-top: 1px solid #e5e7eb;
            background-color: white;
            padding: 0.75rem 0;
            text-align: center;
            font-size: 0.875rem;
            color: #6b7280;
            flex-shrink: 0;
        }

    </style>
</head>
<body>

    <header class="top-header">
        <h1 class="top-title">Student Portal</h1>
        <img src="/student.png" alt="Profile" class="header-profile">
    </header>

    <div class="page-container">
        <div class="sidebar">
            <div>
                <div class="nav-title">Navigation</div>
                <nav>
                    <a href="/student/dashboard" class="nav-link <%= activePage === 'dashboard' ? 'active' : '' %>">
                        Dashboard
                    </a>
                    <a href="/student/assignments" class="nav-link <%= activePage === 'assignments' ? 'active' : '' %>">
                        My Assignments
                    </a>
                    <a href="/student/assignments/upload" class="nav-link <%= activePage === 'submit' ? 'active' : '' %>">
                        Submit New
                    </a>
                    <a href="#" class="nav-link">
                        Profile
                    </a>
                </nav>
            </div>
            <form action="/logout" method="POST">
                <button type="submit" class="logout-button">
                    <div class="sign">
                        <svg viewBox="0 0 512 512">
                            <path d="M377.9 105.9L500.7 228.7c7.2 7.2 11.3 17.1 11.3 27.3s-4.1 20.1-11.3 27.3L377.9 406.1c-6.4 6.4-15 9.9-24 9.9c-18.7 0-33.9-15.2-33.9-33.9l0-62.1-128 0c-17.7 0-32-14.3-32-32l0-64c0-17.7 14.3-32 32-32l128 0 0-62.1c0-18.7 15.2-33.9 33.9-33.9c9 0 17.6 3.6 24 9.9zM160 96L96 96c-17.7 0-32 14.3-32 32l0 256c0 17.7 14.3 32 32 32l64 0c17.7 0 32 14.3 32 32s-14.3 32-32 32l-64 0c-53 0-96-43-96-96L0 128C0 75 43 32 96 32l64 0c17.7 0 32 14.3 32 32s-14.3 32-32 32z"></path>
                        </svg>
                    </div>
                    <div class="text">Logout</div>
                </button>
            </form>
        </div>

        <div class="main-content">
            <h1 class="header-title">Upload Assignment</h1>

            <% if (successMessage) { %>
                <div class="success-alert"><%= successMessage %></div>
            <% } %>
            
            <% if (errors.general) { %>
                <div class="error-alert"><%= errors.general %></div>
            <% } %>

            <form action="/student/assignments/upload" method="POST" enctype="multipart/form-data" class="form-card">
                
                <div class="form-group">
                    <label for="title">Assignment Title*</label>
                    <input type="text" id="title" name="title" value="<%= formData.title || '' %>" placeholder="e.g., DBMS Lab 1" required>
                    <% if (errors.title) { %><div class="error-msg"><%= errors.title %></div><% } %>
                </div>

                <div class="form-group">
                    <label for="category">Category*</label>
                    <select id="category" name="category" required>
                        <option value="" disabled <%= !formData.category ? 'selected' : '' %>>Select Category</option>
                        <option value="Assignment" <%= formData.category === 'Assignment' ? 'selected' : '' %>>Assignment</option>
                        <option value="Thesis" <%= formData.category === 'Thesis' ? 'selected' : '' %>>Thesis</option>
                        <option value="Report" <%= formData.category === 'Report' ? 'selected' : '' %>>Report</option>
                    </select>
                    <% if (errors.category) { %><div class="error-msg"><%= errors.category %></div><% } %>
                </div>

                <div class="form-group">
                    <label for="description">Description (Optional)</label>
                    <textarea id="description" name="description" rows="4" placeholder="Any additional details..."><%= formData.description || '' %></textarea>
                </div>

                <div class="form-group">
                    <label for="assignmentFile">Upload File*</label>
                    <input type="file" id="assignmentFile" name="assignmentFile" accept="application/pdf" required>
                    <div class="file-hint">Accepted format: PDF only. Max size: 10MB.</div>
                    <% if (errors.file) { %><div class="error-msg"><%= errors.file %></div><% } %>
                </div>

                <button type="submit" class="submit-btn">Upload Assignment</button>
            
            
                <div style="margin-top: 1.5rem; text-align: center;">
                    <span style="color: #64748b; font-size: 0.9rem;">Need to submit multiple files?</span>
                    <a href="/student/assignments/bulk-upload" style="color: #0f393b; font-weight: 600; font-size: 0.9rem; text-decoration: underline; margin-left: 0.5rem;">
                        Switch to Bulk Upload
                    </a>
                </div>
        </form>
            </form>
        </div>
    </div>

    <footer class="footer">
        &copy; 2025 TaskNet - Student Portal. All rights reserved.
    </footer>

</body>
</html>