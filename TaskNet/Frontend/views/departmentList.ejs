<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Departments - TaskNet</title>
    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,%3Csvg viewBox='0 0 24 24' xmlns='http://www.w3.org/2000/svg'%3E%3Crect width='24' height='24' fill='%23667eea' rx='4'/%3E%3Cpath d='M9 11H7v2h2v-2zm4 0h-2v2h2v-2zm4 0h-2v2h2v-2zm2-7h-1V2h-2v2H8V2H6v2H5c-1.11 0-1.99.9-1.99 2L3 20c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 16H5V9h14v11z' fill='white'/%3E%3C/svg%3E" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #f4f7f9; margin: 0; padding: 0; height: 100vh; display: flex; flex-direction: column; }
        .top-header {
            background-color: #ffffff;
            padding: 1rem 2rem;
            border-bottom: 1px solid #e5e7eb;
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-shrink: 0;
        }

        .header-profile {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid #e5e7eb;
            box-shadow: 0 2px 6px rgba(0,0,0,0.1);
            cursor: pointer;
        }
        .top-title { font-size: 1.875rem; font-weight: 700; color: #1f2937; }
        .page-container { display: flex; flex: 1; overflow: hidden; }
        .sidebar { width: 256px; display: flex; flex-direction: column; justify-content: space-between; padding: 1.5rem; background-color: #ffffff; border-right: 1px solid #e5e7eb; box-sizing: border-box; }
        .main-content { flex-grow: 1; overflow-x: hidden; overflow-y: auto; padding: 2rem; background-color: #f9fafb; }
        .nav-link { display: block; padding: 1rem; margin-bottom: 0.5rem; border-radius: 0.5rem; color: #4b5563; font-weight: 600; transition: all 0.15s ease-in-out; text-decoration: none; }
        .nav-link:hover { background-color: #f3f4f6; color: #1f2937; }
        .nav-link.active { background-color: #e5e7eb; color: #1f2937; }
        .logout-button { display: flex; align-items: center; justify-content: flex-start; width: 45px; height: 45px; border: none; border-radius: 50%; cursor: pointer; position: relative; overflow: hidden; transition-duration: 0.3s; box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.199); background-color: white; margin-top: 2rem; }
        .logout-button .sign { width: 100%; transition-duration: 0.3s; display: flex; align-items: center; justify-content: center; }
        .logout-button .sign svg { width: 17px; }
        .logout-button .sign svg path { fill: black; }
        .logout-button .text { position: absolute; right: 0%; width: 0%; opacity: 0; color: white; font-size: 1.2em; font-weight: 600; transition-duration: 0.3s; letter-spacing: 0.05em; }
        .logout-button:hover { background-color: black; width: 155px; border-radius: 40px; transition-duration: 0.3s; }
        .logout-button:hover .sign { width: 25%; transition-duration: 0.3s; padding-left: 15px; }
        .logout-button:hover .sign svg path { fill: white; }
        .logout-button:hover .text { opacity: 1; width: 75%; transition-duration: 0.3s; padding-right: 15px; text-align: center; }
        .logout-button:active { transform: translate(2px, 2px); }
        .header { padding-bottom: 1.5rem; margin-bottom: 2rem; border-bottom: 1px solid #e5e7eb; }
        .content-title { font-size: 1.5rem; font-weight: 700; color: #1f2937; }
        .nav-title { font-size: 1.125rem; font-weight: 600; color: #374151; }
        .welcome-text { color: #4b5563; margin-top: 0.25rem; font-size: 1rem;}
        .welcome-user { font-weight: 600; color: #1f2937; }
        .department-actions { margin-bottom: 1.5rem; }
        .create-button { padding: 0.5rem 1rem; background-color: #4f46e5; color: white; border: none; border-radius: 0.5rem; font-weight: 600; cursor: pointer; text-decoration: none; display: inline-block; transition: background-color 0.2s; }
        .create-button:hover { background-color: #4338ca; }
        .department-table { width: 100%; border-collapse: collapse; background-color: white; border-radius: 0.75rem; overflow: hidden; box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05); margin-bottom: 1.5rem; }
        .department-table th, .department-table td { padding: 1rem; text-align: left; border-bottom: 1px solid #f3f4f6; }
        .department-table th { background-color: #f9fafb; font-weight: 700; color: #4b5563; text-transform: uppercase; font-size: 0.875rem; }
        .department-table tbody tr:hover { background-color: #f3f4f6; }
        .department-table tbody tr:last-child td { border-bottom: none; }
        .no-data { padding: 2rem; text-align: center; color: #6b7280; background-color: white; border-radius: 0.75rem; }
        .action-button { padding: 0.4rem 0.8rem; border-radius: 0.3rem; font-weight: 600; cursor: pointer; text-decoration: none; display: inline-block; margin-right: 0.5rem; transition: background-color 0.2s; }
        .edit-button { background-color: #3b82f6; color: white; border: 1px solid #3b82f6; }
        .edit-button:hover { background-color: #2563eb; }
        .delete-button { background-color: transparent; color: #ef4444; border: 1px solid #ef4444; }
        .delete-button:hover { background-color: #fee2e2; }
        .pagination { display: flex; justify-content: center; align-items: center; gap: 0.5rem; margin-top: 1rem; }
        .pagination-button { padding: 0.5rem 1rem; background-color: #ffffff; border: 1px solid #d1d5db; border-radius: 0.5rem; cursor: pointer; transition: background-color 0.2s; text-decoration: none; color: #1f2937; font-weight: 600; }
        .pagination-button:hover:not(.disabled):not(.active) { background-color: #f3f4f6; }
        .pagination-button.active { background-color: #4f46e5; color: white; border-color: #4f46e5; }
        .pagination-button.disabled { opacity: 0.5; cursor: not-allowed; }
        .controls-container { display: flex; justify-content: space-between; align-items: flex-end; gap: 1.5rem; margin-bottom: 1.5rem; }
        .search-filter-group { display: flex; gap: 1rem; align-items: flex-end; flex-grow: 1; }
        .form-control-group { display: flex; flex-direction: column; }
        .form-control-group label { margin-bottom: 0.25rem; }
        input[type="text"], select { padding: 0.5rem 1rem; border: 1px solid #d1d5db; border-radius: 0.5rem; box-sizing: border-box; font-size: 1rem; width: 100%; }
        .footer {
            border-top: 1px solid #e5e7eb;
            background-color: white;
            padding: 1rem 0;
            text-align: center;
            font-size: 0.875rem;
            color: #6b7280;
        }
        @media (max-width: 768px) {
             .page-container { flex-direction: column; min-height: auto; }
             .sidebar { width: 100%; border-right: none; border-bottom: 1px solid #e5e7eb; }
             .department-table th, .department-table td { padding: 0.75rem 0.5rem; font-size: 0.75rem; }
             .controls-container { flex-direction: column; align-items: stretch; }
             .search-filter-group { flex-direction: column; align-items: stretch; }
        }

        .modal {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            display: none;
            justify-content: center;
            align-items: center;
            z-index: 1000;
        }
        .modal-content {
            background-color: white;
            padding: 2rem;
            border-radius: 0.75rem;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
            max-width: 550px;
            width: 90%;
            max-height: 90%;
            overflow-y: auto;
        }
        .confirmation-modal-buttons {
            display: flex;
            justify-content: flex-end;
            gap: 1rem;
            margin-top: 1.5rem;
        }
        .modal-close-btn {
            padding: 0.6rem 1.2rem; 
            background-color: #e5e7eb; 
            color: #374151; 
            border: none; 
            border-radius: 0.5rem; 
            font-weight: 600; 
            cursor: pointer;
        }
        .modal-confirm-btn {
            padding: 0.6rem 1.2rem; 
            background-color: #ef4444; 
            color: white; 
            border: none; 
            border-radius: 0.5rem; 
            font-weight: 600; 
            cursor: pointer;
        }
        .success-message-global {
            background-color: #d1fae5; 
            color: #065f46; 
            padding: 1rem; 
            border-radius: 0.5rem; 
            margin-bottom: 1.5rem; 
            font-weight: 600;
        }
        .warning-message-global {
            background-color: #fffbeb; 
            color: #b45309; 
            padding: 1rem; 
            border-radius: 0.5rem; 
            margin-bottom: 1.5rem; 
            font-weight: 600;
        }
    </style>
</head>
<body>

    <header class="top-header">
        <h1 class="top-title">Admin Dashboard</h1>
        <img src="/admin.png" alt="Profile" class="header-profile">
    </header>

    <div class="page-container">
        <div class="sidebar">
            <div>
                <h2 class="nav-title mb-4">Navigation</h2>

                <nav class="flex flex-col">
                    <a href="/admin/dashboard" class="nav-link">
                        Dashboard Overview
                    </a>
                    <a href="/admin/departments" class="nav-link active">
                        Departments
                    </a>
                    <a href="/admin/users" class="nav-link">
                        Users
                    </a>
                    <a href="/admin/add-user" class="nav-link">
                        Add User
                    </a>
                </nav>
            </div>
            
            <div class="mt-8">
                <form action="/logout" method="POST">
                    <button type="submit" class="logout-button">
                        <div class="sign">
                            <svg viewBox="0 0 512 512">
                                <path d="M377.9 105.9L500.7 228.7c7.2 7.2 11.3 17.1 11.3 27.3s-4.1 20.1-11.3 27.3L377.9 406.1c-6.4 6.4-15 9.9-24 9.9c-18.7 0-33.9-15.2-33.9-33.9l0-62.1-128 0c-17.7 0-32-14.3-32-32l0-64c0-17.7 14.3-32 32-32l128 0 0-62.1c0-18.7 15.2-33.9 33.9-33.9c9 0 17.6 3.6 24 9.9zM160 96L96 96c-17.7 0-32 14.3-32 32l0 256c0 17.7 14.3 32 32 32l64 0c17.7 0 32 14.3 32 32s-14.3 32-32 32l-64 0c-53 0-96-43-96-96L0 128C0 75 43 32 96 32l64 0c17.7 0 32 14.3 32 32s-14.3 32-32 32z"></path>
                            </svg>
                        </div>
                        <div class="text">Logout</div>
                    </button>
                </form>
            </div>
        </div>

        <div class="main-content">
            <header class="header">
                <h1 class="content-title">All Departments</h1>
                <div class="welcome-text">Welcome, <span class="welcome-user"><%= user.name %> (<%= user.role %>)</span></div>
            </header>
            
            <% if (error) { %>
                <div class="general-error" style="margin-bottom: 1.5rem; padding: 1rem; background-color: #fee2e2; color: #991b1b; border-radius: 0.5rem;">
                    <%= error %>
                </div>
            <% } %>

            <div id="globalMessageContainer"></div>

            <div class="controls-container">
                <div class="search-filter-group">
                    <div class="form-control-group" style="width: 250px;">
                        <label for="search" style="font-size: 0.875rem; color: #4b5563;">Search</label>
                        <input type="text" id="search" placeholder="Search by department name" value="<%= searchQuery %>">
                    </div>
                    <div class="form-control-group" style="width: 150px;">
                        <label for="typeFilter" style="font-size: 0.875rem; color: #4b5563;">Filter by Type</label>
                        <select id="typeFilter">
                            <option value="All" <%= filterType === 'All' ? 'selected' : '' %>>All</option>
                            <% programTypes.forEach(type => { %>
                                <option value="<%= type %>" <%= filterType === type ? 'selected' : '' %>>
                                    <%= type %>
                                </option>
                            <% }) %>
                        </select>
                    </div>
                </div>

                <div class="department-actions">
                    <a href="/admin/departments/create" class="create-button">
                        + Create New Department
                    </a>
                </div>
            </div>

            <% if (departments.length > 0) { %>
                <table class="department-table">
                    <thead>
                        <tr>
                            <th>Department Name</th>
                            <th>Type</th>
                            <th>Number of Users</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% departments.forEach(dept => { %>
                            <tr>
                                <td><%= dept.name %></td>
                                <td><%= dept.programType %></td>
                                <td><%= dept.userCount %></td>
                                <td>
                                    <button data-dept-id="<%= dept._id %>" class="action-button edit-button open-edit-modal" type="button">Edit</button>
                                    <button data-dept-id="<%= dept._id %>" data-dept-name="<%= dept.name %>" class="action-button delete-button open-delete-modal" type="button">Delete</button>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
                
                <div class="pagination">
                    <a href="?page=<%= currentPage - 1 %>&search=<%= searchQuery %>&type=<%= filterType %>" 
                       class="pagination-button <%= currentPage <= 1 ? 'disabled' : '' %>">
                        Previous
                    </a>

                    <% for(let i = 1; i <= totalPages; i++) { %>
                        <a href="?page=<%= i %>&search=<%= searchQuery %>&type=<%= filterType %>" 
                           class="pagination-button <%= currentPage === i ? 'active' : '' %>">
                            <%= i %>
                        </a>
                    <% } %>

                    <a href="?page=<%= currentPage + 1 %>&search=<%= searchQuery %>&type=<%= filterType %>" 
                       class="pagination-button <%= currentPage >= totalPages ? 'disabled' : '' %>">
                        Next
                    </a>
                </div>

            <% } else { %>
                <div class="no-data">
                    No departments found matching the current criteria.
                </div>
            <% } %>
        </div>
    </div>

    <div id="editModal" class="modal">
        <div id="modalContent" class="modal-content">
        </div>
    </div>
    
    <div id="confirmModal" class="modal">
        <div id="confirmModalContent" class="modal-content">
            <h2 style="font-size: 1.5rem; font-weight: 700; color: #1f2937; margin-bottom: 1rem;" id="confirmTitle">Confirm Deletion</h2>
            <p id="confirmMessage" style="color: #4b5563;">Are you sure you want to delete this department?</p>
            <div class="confirmation-modal-buttons">
                <button type="button" class="modal-close-btn" id="confirmCancel">Cancel</button>
                <button type="button" class="modal-confirm-btn" id="confirmProceed">Delete</button>
            </div>
        </div>
    </div>

    <footer class="footer">
        &copy; 2025 TaskNet - Admin Dashboard. All rights reserved.
    </footer>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const searchInput = document.getElementById('search');
            const typeFilter = document.getElementById('typeFilter');
            const editModal = document.getElementById('editModal');
            const modalContent = document.getElementById('modalContent');
            const confirmModal = document.getElementById('confirmModal');
            const confirmTitle = document.getElementById('confirmTitle');
            const confirmMessage = document.getElementById('confirmMessage');
            const confirmCancel = document.getElementById('confirmCancel');
            const confirmProceed = document.getElementById('confirmProceed');
            const mainContent = document.querySelector('.main-content');
            const globalMessageContainer = document.getElementById('globalMessageContainer');

            const closeEditModal = () => {
                editModal.style.display = 'none';
                modalContent.innerHTML = '';
            };

            const closeConfirmModal = () => {
                confirmModal.style.display = 'none';
                confirmProceed.removeEventListener('click', confirmProceed.clickHandler); // Clean up listener
                confirmProceed.clickHandler = null;
            };

            const displayGlobalMessage = (type, message) => {
                globalMessageContainer.innerHTML = `<div class="${type}-message-global">${message}</div>`;
                setTimeout(() => {
                    globalMessageContainer.innerHTML = '';
                }, 5000);
            };

            const attachModalCloseListeners = () => {
                const closeBtn = modalContent.querySelector('.modal-close-btn');
                if (closeBtn) { closeBtn.addEventListener('click', closeEditModal); }
            };

            const handleEditFormSubmission = (e) => {
                e.preventDefault();
                const form = e.target;
                const formData = new URLSearchParams(new FormData(form));
                
                const submitButton = form.querySelector('.submit-button');
                const originalText = submitButton.textContent;
                submitButton.textContent = 'Saving...';
                submitButton.disabled = true;

                fetch(form.action, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
                    body: formData
                })
                .then(response => response.text())
                .then(html => {
                    modalContent.innerHTML = html;
                    attachModalCloseListeners();
                    
                    const successDiv = modalContent.querySelector('.success-message');

                    if (successDiv) {
                         displayGlobalMessage('success', successDiv.textContent.trim());
                         closeEditModal();
                         window.location.reload(); 
                    } else {
                        const newForm = document.getElementById('editDepartmentForm');
                        if (newForm) {
                            newForm.addEventListener('submit', handleEditFormSubmission);
                        }
                    }
                })
                .catch(error => {
                    console.error('Update failed:', error);
                    displayGlobalMessage('warning', 'Failed to update department due to server error.');
                    closeEditModal();
                });
            };

            const performDelete = (deptId, deptName) => {
                confirmProceed.textContent = 'Deleting...';
                confirmProceed.disabled = true;

                fetch(`/admin/departments/${deptId}/delete`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' }
                })
                .then(response => response.json())
                .then(data => {
                    closeConfirmModal();
                    if (data.success) {
                        displayGlobalMessage('success', data.message);
                        const row = document.getElementById(`dept-row-${deptId}`);
                        if (row){
                            row.remove(); 
                        } else{
                            window.location.reload();
                        }

                    } else {
                        displayGlobalMessage('warning', data.message);
                    }
                })
                .catch(error => {
                    console.error('Delete failed:', error);
                    displayGlobalMessage('warning', `Failed to delete ${deptName}.`);
                    closeConfirmModal();
                })
                .finally(() => {
                    confirmProceed.textContent = 'Delete';
                    confirmProceed.disabled = false;
                });
            };

            const applyFilters = () => {
                const search = searchInput.value;
                const type = typeFilter.value;
                let url = '/admin/departments?page=1'; 
                if (search) { url += `&search=${encodeURIComponent(search)}`; }
                if (type !== 'All') { url += `&type=${encodeURIComponent(type)}`; }
                window.location.href = url;
            };

            typeFilter.addEventListener('change', applyFilters);
            searchInput.addEventListener('keypress', (e) => {
                if (e.key === 'Enter') { applyFilters(); }
            });
            searchInput.addEventListener('blur', applyFilters);
            document.querySelectorAll('.pagination-button.disabled').forEach(button => {
                button.addEventListener('click', (e) => { e.preventDefault(); });
            });


            mainContent.addEventListener('click', (e) => {
                if (e.target.classList.contains('open-edit-modal')) {
                    const deptId = e.target.dataset.deptId;
                    fetch(`/admin/departments/${deptId}/edit`, {
                    credentials: 'same-origin'
                    })
                    .then(response => response.text())
                    .then(html => {
                        modalContent.innerHTML = html;
                        editModal.style.display = 'flex';
                        attachModalCloseListeners();
                        const form = document.getElementById('editDepartmentForm');
                    if (form) {
                        form.addEventListener('submit', handleEditFormSubmission);
                    }
                    })
                .catch(error => {
                    console.error('Error fetching edit form:', error);
                    displayGlobalMessage('warning', 'Could not load edit form.');
                });
                }

                if (e.target.classList.contains('open-delete-modal')) {
                    const deptId = e.target.dataset.deptId;
                    const deptName = e.target.dataset.deptName;

                    confirmTitle.textContent = `Delete ${deptName}?`;
                    confirmMessage.innerHTML = `You are about to permanently delete the department: <strong>${deptName}</strong>. This action cannot be undone.`;
   
                    confirmProceed.clickHandler = () => performDelete(deptId, deptName);
                    confirmProceed.addEventListener('click', confirmProceed.clickHandler);

                    confirmModal.style.display = 'flex';
                }
            });

            editModal.addEventListener('click', (e) => {
                if (e.target === editModal) { closeEditModal(); }
            });
            confirmModal.addEventListener('click', (e) => {
                if (e.target === confirmModal) { closeConfirmModal(); }
            });
            confirmCancel.addEventListener('click', closeConfirmModal);
        });
    </script>
</body>
</html>