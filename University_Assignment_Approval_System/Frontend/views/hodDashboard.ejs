<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>HOD Dashboard</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
        }

        .dashboard-container {
            background-color: #ffffff;
            /* Increased size */
            padding: 50px 70px;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
            width: 90%;
            max-width: 800px;
            border: 5px solid #4caf50; /* Green border */
            text-align: center;
        }

        .role-icon {
            display: block;
            margin: 0 auto 20px auto;
            width: 80px;
            height: 80px;
            background-color: #4caf50;
            border-radius: 50%;
        }

        h1 {
            color: #333;
            text-align: center;
            margin-bottom: 15px;
            font-size: 2.5em;
        }

        p {
            text-align: center;
            color: #666;
            margin-top: 0;
            margin-bottom: 40px;
            font-size: 1.2em;
        }

        strong {
            color: #4caf50;
        }

        ul {
            list-style-type: none;
            padding: 0;
            margin-top: 30px;
        }

        li {
            background-color: #e8f5e9; /* Light green background */
            margin-bottom: 15px;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
            transition: background-color 0.3s;
        }

        li:hover {
            background-color: #c8e6c9;
        }

        h2 {
            color: #4caf50;
            font-size: 1.8em;
            margin: 0;
        }

        .logoutBtn {
            display: block;
            width: 100%;
            padding: 18px;
            margin-top: 40px;
            background-color: #4caf50;
            color: white;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            font-size: 1.4em;
            transition: background-color 0.3s;
        }

        .logoutBtn:hover {
            background-color: #388e3c;
        }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <img src="/hod.png" alt="HOD Icon" class="role-icon" />
        <h1>Welcome, <%= user.name %> </h1>
        <p>You are logged in as <strong><%= user.role %></strong></p>

        <div>
            <ul>
                <li><h2>Final approval</h2></li>
                <li><h2>Escalating handling</h2></li>
            </ul>
        </div>

        <form action="/logout" method="POST">
            <button type="submit" class="logoutBtn">Logout</button>
        </form>
    </div>
</body>
</html>